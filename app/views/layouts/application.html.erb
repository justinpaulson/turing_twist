<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Turing Twist" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <%= favicon_link_tag "favicon.png", type: "image/png" %>
    <link rel="apple-touch-icon" href="<%= asset_path('favicon.png') %>">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header style="background: #000000; color: #ffffff; padding: 1rem; border-bottom: 4px solid #000000;">
      <div style="max-width: 1200px; margin: 0 auto; display: flex; justify-content: space-between; align-items: center;">
        <h1 style="margin: 0; font-size: 1.75rem; font-weight: 900; text-transform: uppercase; letter-spacing: 2px;">
          <%= link_to "TURING TWIST", root_path, style: "color: #ffffff; text-decoration: none;" %>
        </h1>
        <nav style="display: flex; gap: 1.5rem; align-items: center; font-family: 'Courier New', monospace;">
          <% if authenticated? %>
            <span style="color: #ffffff; font-size: 0.9rem;">
              <%= Current.user.email_address %>
            </span>
            <%= link_to "SIGN OUT", session_path, data: { turbo_method: :delete }, style: "color: #ffffff; text-decoration: none; font-weight: bold; border: 2px solid #ffffff; padding: 0.5rem 1rem;" %>
          <% else %>
            <%= link_to "SIGN IN", new_session_path, style: "color: #ffffff; text-decoration: none; font-weight: bold; border: 2px solid #ffffff; padding: 0.5rem 1rem;" %>
          <% end %>
        </nav>
      </div>
    </header>

    <main style="max-width: 1200px; margin: 2rem auto; padding: 0 1rem;">
      <% if notice.present? %>
        <div class="notice">
          ℹ <%= notice %>
        </div>
      <% end %>
      <% if alert.present? %>
        <div class="alert">
          ⚠ <%= alert %>
        </div>
      <% end %>

      <%= yield %>
    </main>

    <footer style="max-width: 1200px; margin: 3rem auto 1rem; padding: 1rem; text-align: center; border-top: 4px solid #000000; font-family: 'Courier New', monospace; font-size: 0.8rem;">
      © 2025 TURING TWIST
    </footer>

    <%# tip_jar payment_link: 'https://buy.stripe.com/00w6oG1JM7Rua1hd7kfrW04',
                color: '#000000' %>
  </body>
</html>
