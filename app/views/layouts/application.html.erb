<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Turing Twist" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%# Open Graph meta tags for link sharing %>
    <meta property="og:title" content="<%= content_for(:title) || "Turing Twist" %>">
    <meta property="og:description" content="A retro-style party game inspired by classic Nintendo and newspaper aesthetics">
    <meta property="og:image" content="<%= asset_url('favicon.png') %>">
    <meta property="og:image:type" content="image/png">
    <meta property="og:type" content="website">
    <meta property="og:url" content="<%= request.original_url %>">

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <%= favicon_link_tag "favicon.png", type: "image/png" %>
    <link rel="apple-touch-icon" href="<%= asset_path('favicon.png') %>">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <style>
      .header-container {
        max-width: 1200px;
        margin: 0 auto;
        display: flex;
        justify-content: space-between;
        align-items: center;
      }
      .header-container h1 {
        margin: 0;
        padding: 0;
        border: none;
        line-height: 1;
        display: flex;
        align-items: center;
      }
      .header-container h1 a {
        display: inline-block;
        line-height: 1;
      }
      .header-email {
        color: #ffffff;
        font-size: 0.9rem;
        line-height: 1;
        text-decoration: none;
        transition: opacity 0.2s;
      }
      .header-email:hover {
        opacity: 0.8;
      }
      .header-nav {
        display: flex;
        gap: 1.5rem;
        align-items: center;
        font-family: 'Courier New', monospace;
      }
      .header-button {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        line-height: 1;
        color: #ffffff;
        text-decoration: none;
        font-weight: bold;
        border: 2px solid #ffffff;
        padding: 0.5rem 1rem;
        box-sizing: border-box;
      }
      @media (max-width: 768px) {
        .header-sign-out {
          display: none !important;
        }
        .header-container h1 {
          font-size: 1.25rem !important;
        }
        .header-nav {
          gap: 0.5rem !important;
        }
        .header-button {
          padding: 0.4rem 0.8rem !important;
          font-size: 0.85rem;
        }
      }
    </style>
    <header style="background: #000000; color: #ffffff; padding: 1rem; border-bottom: 4px solid #000000;">
      <div class="header-container">
        <h1 style="font-size: 1.75rem; font-weight: 900; text-transform: uppercase; letter-spacing: 2px;">
          <%= link_to "TURING TWIST", root_path, style: "color: #ffffff; text-decoration: none;" %>
        </h1>
        <nav class="header-nav">
          <% if authenticated? %>
            <%= link_to Current.user.display_name || Current.user.email_address, edit_profile_path, class: "header-email" %>
            <%= link_to "SIGN OUT", session_path, data: { turbo_method: :delete }, class: "header-button header-sign-out" %>
          <% else %>
            <%= link_to "SIGN IN", new_session_path, class: "header-button" %>
          <% end %>
        </nav>
      </div>
    </header>

    <main style="max-width: 1200px; margin: 2rem auto; padding: 0 1rem;">
      <% if notice.present? %>
        <div class="notice">
          ℹ <%= notice %>
        </div>
      <% end %>
      <% if alert.present? %>
        <div class="alert">
          ⚠ <%= alert %>
        </div>
      <% end %>

      <%= yield %>
    </main>

    <footer style="max-width: 1200px; margin: 3rem auto 1rem; padding: 1rem; text-align: center; border-top: 4px solid #000000; font-family: 'Courier New', monospace; font-size: 0.8rem;">
      © 2025 TURING TWIST
    </footer>

    <%= tip_jar payment_link: 'https://buy.stripe.com/00w6oG1JM7Rua1hd7kfrW04',
                pulse: true,
                color: '#000000' %>
  </body>
</html>
